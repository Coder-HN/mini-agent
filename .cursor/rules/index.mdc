---
description: "项目核心编码规范 - 低耦合、最小影响、避免过度封装"
alwaysApply: true
---

# 核心编码原则

## 最小影响
- 优先精准修改（如单函数），严禁无脑重写整个文件
- 新增功能优先创建独立模块，不强行塞入现有逻辑

## 低耦合
- 优先纯函数和无状态组件
- 避免全局变量和长继承链
- 模块间通过明确参数/返回值通信

## 避免过度封装
- 只有当下需要复用的代码才抽取
- 保持扁平，简单逻辑写在同一文件

## 代码生成
- 风格与现有项目保持一致
- 优先选择**最小改动**的方案

## 同步代码时
严格按参照代码同步，禁止引入新写法或"优化"，仅修改不一致部分

## 改结构必改 README
增删/移动文件或文件夹时，同步更新 README.md 中的项目结构树

# 代码注释规范（摘要）

编写或修改代码注释时，遵循项目根目录 `documents/code-comments-standard.md`（若存在）。
要点：
- 解释 **为什么**、边界与安全/业务约束，避免复述代码。
- **导出/公共** API 优先；Go 用 doc 注释；PHP 用 docblock；TS 导出符号用 JSDoc/TSDoc。
- 与本仓库一致：**中文为主**（同一文件内语言统一），专有名词可英文。
- 迁移 SQL、危险 Shell **必须**有文件头或关键步骤说明意图与风险。
- 注释与代码同步更新；禁止在注释中写真实密钥。
