---
description: 防止代码过度封装、过度抽象、过度重构，保持代码收敛可读
alwaysApply: true
---

# 代码收敛原则（防过度工程化）

## 适用边界

- 本规则用于约束**新增代码和本次改动**，不要求主动整理历史遗留代码
- 修改遗留大文件时，只收敛本次改动；不因为存量超限而扩大化重构
- 必要的测试、配置、迁移、文档可算作任务范围内，但必须说明修改原因

## 第一原则：只做被要求的事

- 只修改完成任务所必须的代码
- 禁止修改任务范围以外的文件
- 禁止"顺便重构"、"顺便优化"、"顺便统一"
- 如果发现更好的设计，**先说明，不要直接实现**

## 新建文件的约束

- 如果任务可以通过修改现有文件完成，**禁止新建文件**
- 新建文件前必须确认：现有文件中确实无法容纳，而非"为了整洁"
- 禁止为单次使用或假想复用新建：`*Util`、`*Helper`、`*Manager`、`*Handler`、`*Constants`、`*Types`
- 如果现有目录或框架已有同类命名约定，可以沿用，但不得借此新增无实际职责的中间层

## 抽象的约束

**Rule of Three（三次原则）**：第一次 inline，第二次忍住，**第三次**才允许抽象。

禁止引入以下内容，除非明确被要求：
- 基类 / 抽象类（`Base*`、`Abstract*`）
- 工厂、策略、适配器、装饰器等设计模式
- 泛型框架、插件系统、Provider/Registry/Resolver
- 透传层（A 调 B 调 C，中间层无实际逻辑）

**YAGNI**：不为"将来可能需要"的场景写代码，只解决当下真实问题。

## 代码结构的约束

| 指标 | 上限 |
|------|------|
| 新增文件行数 | 尽量不超过 2000 行 |
| 函数行数 | 40 行 |
| 嵌套深度 | 3 层 |
| 函数参数数 | 4 个 |
| 圈复杂度 | 10 |

超过红线时，优先用 Guard Clause（提前返回）、删除重复逻辑、收敛本次改动来简化，而非新增封装层。遗留文件已超过上限时，不因此主动拆文件。

## 编码优先级

1. **可读性 > DRY**：三行重复比五层泛型好维护；避免为消除重复引入难以理解的抽象
2. **编辑 > 重写**：优先修改现有函数，禁止对可用的实现整体重写
3. **简单 > 聪明**：`if/for/switch` 优先于函数式管道、元编程、泛型抽象
4. **局部性**：相关逻辑集中在一处，理解一个功能不应跨超过 2 个文件

## 写代码前必须做的事

- 先搜索现有实现，**复用优先于新建**，避免出现多套同功能实现
- 确认本次修改范围：哪些文件会变、为什么变

## 完成代码修改后必须输出

```
变更文件：
潜在风险：
是否有 Breaking Change：
```

## 判断口诀

> 删掉这层封装把代码 inline 回调用处，可读性不下降甚至提升 → 不应该封装。
>
> 任务能改现有文件完成 → 不应该新建文件。
>
> Diff 越小越好。
