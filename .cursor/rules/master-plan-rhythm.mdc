---
description: 按系统总体规划逐步推进（一句话触发、一回合一切片）
alwaysApply: true
---

# 系统总体规划：固定节奏

真源：[documents/系统总体规划.md](documents/系统总体规划.md)（含各子项目本地路径）。用户一句话要求「按总体规划推进 / 做下一里程碑 / 继续 M*」时，必须按本规则行动，不要一次做完整份文档。

## 触发后立刻做

1. 读 `documents/系统总体规划.md`，找出**下一个未勾完**的里程碑（默认顺序 M0→M1→M2→M3→M4→M5）。
2. 本回合只做一个切片：**一个里程碑 × 一个仓库**（跨仓则拆多回合）。
3. 先 OpenSpec（当事仓 `propose` → `apply`），再改代码；禁止无 change 直接大改。
4. 做完：勾该仓 `openspec/changes/.../tasks.md`；同步勾总体规划对应 `[ ]`；汇报验收与下一切片建议。

## 切片默认顺序（侧重后端 + Agent）

| 顺序 | 切片 | 仓库 |
|------|------|------|
| 1 | M0 清实习文案 / 回归口径 | 相关仓最小改动 |
| 2 | M1 Go 代理 `/api/v1/agent/chat` | `go-admin-master` |
| 3 | M1 只读工具 + JWT 透传 | `mini-agent` |
| 4 | M1 验收（curl 优先；UI 可后置） | 按需薄改 react |
| 5 | M2 写工具 + 二次确认 | `mini-agent`（+ 控制面若需要） |
| 6 | M3 轨迹 + Web 回放 | `mini-agent` → `go-admin-react` |
| 7 | M4 Electron 挂同一代理 | `go-admin-electron` |
| 8 | M5 门面 | 文档 / compose |

用户指定「只做 M1 / 先 Go / 先 Agent」时，以用户指定为准。

## 硬约束

- 一回合不横跨两个里程碑，不顺手做下一 M。
- React / Electron 默认可后置；用户未要求 UI 时用 curl/CMD 验收。
- 产品故事只维护总体规划；行为契约写当事仓 `openspec/`。
- 遵守既有代码收敛规则（最小改动、禁止顺便重构）。

## 用户一句话示例 → 你的行为

- 「按总体规划继续」→ 读文档 → 做表中下一未完成切片 → OpenSpec → 实现 → 勾选。
- 「做 M1」→ 若 Go 代理未完则只做 Go；已完则做 mini-agent 工具。
- 「M1 用 curl 验收」→ 不写前端，只给/跑验收命令。
